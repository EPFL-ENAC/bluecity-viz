data_folder/
data_folder/*
frontend/public/geodata

processing/**/*.csv
processing/**/*.json
!processing/**/*config.json
processing/**/*.geojson
processing/**/*.shp
processing/**/*.shx
processing/**/*.prj
processing/**/*.dbf
processing/**/*.zip
processing/**/*.gdb
processing/**/*.gpkg
processing/**/*.gpkg-shm
processing/**/*.gpkg-wal
processing/**/*.sqlite
processing/**/*.docx
processing/**/*.pdf
processing/**/*.xlsx
processing/**/*.txt
processing/**/*.xls
processing/**/*.xml
processing/**/*.lock
processing/**/*.cpg
processing/**/*.sbn
processing/**/*.sbx
processing/**/*.qgz
processing/**/*.aprx
processing/**/*.atpx
processing/**/*.pmtiles
processing/**/*.graphml

processing/**/BlueCity_Demo2/**
processing/**/*/*.pbf
processing/traffic-analysis/cache/

# Local settings, read by the root Makefile (see .env.example)
.env

# --- git worktree dev environment (docs/worktree-env/)

# Claude Code local settings, rendered per worktree by scripts/wt-setup.sh
.claude/settings.local.json
# Worktrees live inside the repo (see ~/.config/wt/config.toml)
.claude/worktrees/

# Per-worktree ports written by scripts/wt-setup.sh
.env.worktree

# Server pane output mirrored by scripts/tmux-dev.sh (pipe-pane)
.wt-logs/

# One-shot agent briefs (wtgo --prompt) and local brief drafts
PROMPT.md
PROMPT.sent.md
.wt-prompts/

# wtx: per-worktree files, local to one checkout
.claude/agents/Explore.md
opencode.json
