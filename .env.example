# Copy to .env and fill in. Gitignored, the root Makefile reads it.
# One KEY=value per line, no quotes around the value.

# The S3 bucket `make upload-frontend-geodata` pushes frontend/public/geodata
# to. The files then answer under https://<BUCKET_NAME>/bluecity/.
BUCKET_NAME=
